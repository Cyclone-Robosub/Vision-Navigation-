#!/bin/bash
echo "[INFO] Setting up project dependencies..."

# Install required packages (Linux example)
sudo apt-get update
sudo apt-get install -y build-essential cmake libopencv-dev

echo "[INFO] Setup complete!"
